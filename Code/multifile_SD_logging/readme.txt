This script is an implementation of a data logging for the BoSL board. It has functionality for the SD card, and saves the readings every minute on the card.

The library "BoSL-lib" must be installed for this to work.